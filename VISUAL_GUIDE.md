# 🎨 Visual Integration Guide

## Dòng Dữ Liệu (Data Flow)

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Your Excel File                              │
│                        (tudien.xlsx)                                 │
│                                                                      │
│  ┌──────────┬──────────┬────────────┬──────────┐                    │
│  │    B     │    C     │     E      │    F     │  ...other columns  │
│  │---------┼──────────┼────────────┼──────────┤                    │
│  │original │  ndict   │  phat_hc   │  tdict   │                    │
│  │---------┼──────────┼────────────┼──────────┤                    │
│  │ hello   │xin chào  │ hə'ləʊ     │  ...     │                    │
│  │ world   │thế giới  │ wɜːld      │  ...     │                    │
│  └──────────┴──────────┴────────────┴──────────┘                    │
│                    ↓                                                   │
└─────────────────────────────────────────────────────────────────────┘
                     ↓
            ┌────────────────────┐
            │  Select & Extract  │
            │ - original (col B) │
            │ - ndict (col C)    │
            │ - phat_hc (col E)  │
            │ - full_data (all)  │
            └────────────────────┘
                     ↓
    ┌────────────────────────────────────────┐
    │  app/api/dictionary/import/route.ts    │
    │                                        │
    │  POST /api/dictionary/import           │
    │  - File upload                         │
    │  - Column mapping                      │
    │  - Data validation                     │
    │  - Database import                     │
    └────────────────────────────────────────┘
                     ↓
         ┌───────────────────────┐
         │  SQLite Database      │
         │  (dictionary.db)      │
         │                       │
         │  ┌─────────────────┐  │
         │  │  words table    │  │
         │  ├─────────────────┤  │
         │  │ id              │  │
         │  │ original    ←─┐ │  │
         │  │ ndict       ←─┼─┤  │
         │  │ phat_hc     ←─┘ │  │
         │  │ full_data       │  │
         │  │ created_at      │  │
         │  │ updated_at      │  │
         │  └─────────────────┘  │
         └───────────────────────┘
                     ↓
    ┌────────────────────────────────────────┐
    │  app/api/dictionary/search/route.ts    │
    │                                        │
    │  GET /api/dictionary/search?q=hello    │
    │  - Query parameter: q=hello            │
    │  - Search original column (index)      │
    │  - Return: { results: [Word] }         │
    └────────────────────────────────────────┘
                     ↓
    ┌────────────────────────────────────────┐
    │  app/components/SearchForm.tsx         │
    │                                        │
    │  Display Results:                      │
    │  ┌──────────────────────────────────┐  │
    │  │ hello                            │  │
    │  │ /hə'ləʊ/                         │  │
    │  │                                  │  │
    │  │ Nghĩa: xin chào                  │  │
    │  └──────────────────────────────────┘  │
    │                                        │
    │  Fields used:                          │
    │  - word.original → Title               │
    │  - word.phat_hc → Pronunciation        │
    │  - word.ndict → Meaning                │
    └────────────────────────────────────────┘
                     ↓
         ┌──────────────────────┐
         │   Browser Display    │
         │  (http://localhost)  │
         │                      │
         │  ┌────────────────┐  │
         │  │ hello          │  │
         │  │ /hə'ləʊ/       │  │
         │  │ Nghĩa: xin... │  │
         │  └────────────────┘  │
         └──────────────────────┘
```

---

## 🔄 Process Workflow

### 1. Import Phase

```
User Action: Upload Excel file
         ↓
Form Validation
  ✓ File exists
  ✓ File format (.xlsx, .xls, .csv)
         ↓
Read Excel Workbook
  ✓ Parse first sheet
  ✓ Read all rows
         ↓
Extract & Map Columns
  - Get row.original (Column B)
  - Get row.ndict (Column C)
  - Get row.phat_hc (Column E)
  - Store entire row as JSON
         ↓
Validate Data
  ✓ original field not empty
  ✓ original is unique
         ↓
Database Operation
  - BEGIN TRANSACTION
  - For each word:
    IF replace mode: UPDATE or INSERT
    IF skip mode: INSERT only if new
  - COMMIT TRANSACTION
         ↓
Response
  {
    success: true,
    added: 100,
    updated: 50,
    total: 150
  }
```

### 2. Search Phase

```
User Action: Type search query
         ↓
Query Validation
  ✓ Not empty
  ✓ Min length 1
         ↓
Database Query
  SELECT * FROM words
  WHERE lower(original) LIKE lower('%query%')
  ORDER BY original ASC
  LIMIT 20
         ↓
Index Usage
  (Uses idx_original_lower for fast search)
         ↓
Results Returned
  [
    { id, original, ndict, phat_hc, full_data, ... },
    { ... },
    { ... }
  ]
         ↓
Component Rendering
  For each result:
    Display original (bold)
    Display phat_hc (smaller, gray)
    Display ndict (regular)
         ↓
User sees results
```

---

## 📊 Database Schema Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    SQLite Database                          │
│                  (data/dictionary.db)                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                    TABLE: words                       │  │
│  ├─────────┬──────────┬──────────────────────────────────┤  │
│  │ Column  │ Type     │ Description                      │  │
│  ├─────────┼──────────┼──────────────────────────────────┤  │
│  │ id      │ INTEGER  │ Primary Key (auto increment)     │  │
│  │ original│ TEXT     │ English word (from Excel Col B)  │  │
│  │ ndict   │ TEXT     │ Vietnamese (from Excel Col C)    │  │
│  │ phat_hc │ TEXT     │ Pronunciation (from Excel Col E) │  │
│  │ full_dat│ TEXT     │ JSON: All original row data      │  │
│  │ created │ DATETIME │ Import timestamp                 │  │
│  │ updated │ DATETIME │ Last update timestamp            │  │
│  └─────────┴──────────┴──────────────────────────────────┘  │
│                                                             │
│  Constraints:                                              │
│  ├─ PRIMARY KEY: id                                       │
│  ├─ UNIQUE: original                                      │
│  └─ NOT NULL: original                                    │
│                                                             │
│  Indexes:                                                  │
│  ├─ idx_original ON words(original)                      │
│  └─ idx_original_lower ON words(lower(original))         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ API Architecture

```
┌─────────────────────────────────────────────────────────┐
│                  Next.js Routes                         │
│                 (app/api/*)                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ POST /api/dictionary/import                      │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ Input:  File (Excel/CSV) + replace mode         │  │
│  │ Output: { added, updated, total }               │  │
│  │ Uses:   XLSX library → Database operations      │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ GET /api/dictionary/search                       │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ Input:  q (query string) + limit                │  │
│  │ Output: { query, count, results: [Words] }      │  │
│  │ Uses:   Database search (indexed)               │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ GET /api/dictionary/stats                        │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ Input:  (none)                                  │  │
│  │ Output: { totalWords, firstWord, lastWord }     │  │
│  │ Uses:   Database aggregation                    │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
         ↓↓↓
┌─────────────────────────────────────────────────────────┐
│               lib/db/operations.ts                      │
│        (DictionaryOperations class)                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Methods:                                              │
│  ├─ importWords(words[], replace)                     │
│  ├─ searchWords(query, limit)                         │
│  ├─ getWordByOriginal(original)                       │
│  ├─ addWord(word)                                     │
│  ├─ updateWord(id, updates)                           │
│  ├─ deleteWord(id)                                    │
│  └─ getStats()                                        │
│                                                         │
│  Uses: better-sqlite3 Database                        │
│                                                         │
└─────────────────────────────────────────────────────────┘
         ↓↓↓
┌─────────────────────────────────────────────────────────┐
│              data/dictionary.db                         │
│           (SQLite Database)                            │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Component Interaction

```
┌─────────────────────────────────────────────┐
│            React Components                 │
├─────────────────────────────────────────────┤
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  app/page.tsx                          │ │
│  │  (Home page)                           │ │
│  │                                        │ │
│  │  <SearchForm />                        │ │
│  └────────────────────────────────────────┘ │
│              ↓                              │
│  ┌────────────────────────────────────────┐ │
│  │  app/components/SearchForm.tsx         │ │
│  │  (Search interface)                    │ │
│  │                                        │ │
│  │  ├─ Search Input Box                   │ │
│  │  │  └─ onChange → handleSearch()       │ │
│  │  │                                     │ │
│  │  ├─ Results List                       │ │
│  │  │  └─ map(word) → Display component   │ │
│  │  │     ├─ word.original (title)        │ │
│  │  │     ├─ word.phat_hc (pronunciation) │ │
│  │  │     └─ word.ndict (meaning)         │ │
│  │  │                                     │ │
│  │  └─ Stats Box                          │ │
│  │     └─ Fetched from /api/stats         │ │
│  └────────────────────────────────────────┘ │
│              ↓                              │
│         API Calls:                         │
│         - fetch('/api/dictionary/search?')  │
│         - fetch('/api/dictionary/stats')    │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 🔀 Import & Search Flow Comparison

```
IMPORT FLOW                          SEARCH FLOW
───────────────                      ────────────

User uploads Excel file              User types search query
         ↓                                   ↓
Form validation                      Query validation
         ↓                                   ↓
Read Excel sheets                    Format query parameter
         ↓                                   ↓
Extract columns (B,C,E)              Send GET request
         ↓                                   ↓
Map to Word objects                  Database query
         ↓                                   ↓
Validate uniqueness                  Use index (idx_original_lower)
         ↓                                   ↓
Transaction INSERT/UPDATE            Return matched words
         ↓                                   ↓
Response: {added, updated}           Response: [Words]
         ↓                                   ↓
Display confirmation                 Render result cards
```

---

## 📱 UI Mockup - Search Result

```
┌─────────────────────────────────────────────────────────┐
│  Dictionary Search                            [logo]    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 🔍 Nhập từ cần tìm...                  [loading]  │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌──────────────┬──────────────┬──────────────────────┐ │
│  │   500 từ     │   Từ đầu     │    Từ cuối           │ │
│  │   entries    │    start     │    end               │ │
│  └──────────────┴──────────────┴──────────────────────┘ │
│                                                         │
│  Tìm thấy 2 kết quả                                    │
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │                                                    │ │
│  │  hello                                             │ │
│  │  /hə'ləʊ/                                          │ │
│  │                                                    │ │
│  │  Nghĩa: xin chào                                  │ │
│  │                                                    │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │                                                    │ │
│  │  hello world                                       │ │
│  │  /həˈloː wɜːld/                                    │ │
│  │                                                    │ │
│  │  Nghĩa: chào thế giới                             │ │
│  │                                                    │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🔌 API Request/Response Examples

### Import

```
REQUEST:
POST /api/dictionary/import
Content-Type: multipart/form-data

file: [tudien.xlsx]
replace: true

RESPONSE:
{
  "success": true,
  "message": "Import successful. Added: 500, Updated: 0",
  "stats": {
    "added": 500,
    "updated": 0,
    "total": 500
  }
}
```

### Search

```
REQUEST:
GET /api/dictionary/search?q=hello&limit=10

RESPONSE:
{
  "success": true,
  "query": "hello",
  "count": 2,
  "results": [
    {
      "id": 1,
      "original": "hello",
      "ndict": "xin chào",
      "phat_hc": "hə'ləʊ",
      "full_data": {...},
      "created_at": "2024-01-01T00:00:00Z",
      "updated_at": "2024-01-01T00:00:00Z"
    },
    {
      "id": 2,
      "original": "hello world",
      "ndict": "chào thế giới",
      "phat_hc": "hə'ləʊ wɜːld",
      "full_data": {...},
      "created_at": "2024-01-01T00:00:05Z",
      "updated_at": "2024-01-01T00:00:05Z"
    }
  ]
}
```

### Stats

```
REQUEST:
GET /api/dictionary/stats

RESPONSE:
{
  "success": true,
  "data": {
    "totalWords": 500,
    "firstWord": "a",
    "lastWord": "zoo",
    "lastUpdated": "2024-01-01T00:00:00Z"
  }
}
```

---

## ✅ System Ready

All components are connected and tested:

- ✅ Excel Import → Database
- ✅ Search Query → Database Search
- ✅ Results → Component Display
- ✅ UI → Browser Rendering

**System Status: OPERATIONAL** 🚀
